
server {
    location ${URL_PREFIX}/css/ { root ${FILE_PREFIX}; }
    location ${URL_PREFIX}/js/ { root ${FILE_PREFIX}; }
    location / {
        root ${FILE_PREFIX}/html;
        try_files $uri.html =404;
    }
}
